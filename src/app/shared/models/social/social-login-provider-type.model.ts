import { SocialLoginProvider } from './social-login-provider.model';

export interface SocialLoginProviderType {
  fb: SocialLoginProvider;
  google: SocialLoginProvider;
  messenger: SocialLoginProvider;
  whatsapp: SocialLoginProvider;
}
